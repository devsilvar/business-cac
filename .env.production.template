# ============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================
# ⚠️  SECURITY CRITICAL: This file contains production secrets
# - Copy this file to .env.production
# - Fill in all values with REAL production credentials
# - NEVER commit .env.production to git
# - Keep this file SECURE and PRIVATE
# ============================================

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3000
NODE_ENV=production

# ============================================
# ADMIN CREDENTIALS
# ============================================
# ⚠️  CRITICAL: Use strong, unique credentials
# Generate secure password: https://passwordsgenerator.net/ (64+ chars)
# Generate JWT secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=<REPLACE-WITH-STRONG-64-CHAR-PASSWORD>
JWT_SECRET=<REPLACE-WITH-64-CHAR-RANDOM-HEX-STRING>
JWT_EXPIRY=24h

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Production MUST use Firestore (not memory!)
DATABASE_TYPE=firestore

# Firestore Configuration
GOOGLE_CLOUD_PROJECT=<your-firebase-project-id>
FIREBASE_PROJECT_ID=<your-firebase-project-id>
# Service account key file (Firebase Functions uses default credentials)
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# ============================================
# EXTERNAL API CONFIGURATION
# ============================================

# Documents.com.ng API
DOCUMENTS_API_URL=https://app.documents.com.ng
DOCUMENTS_AGENT_ID=<your-documents-agent-id>
DOCUMENTS_API_TIMEOUT=30000

# CAC API (Public endpoints - no auth needed)
CAC_API_URL=https://icrp.cac.gov.ng/name_similarity_app/api/public_search/search
CAC_API_TIMEOUT=15000

# CAC Store API (Public endpoints)
CAC_STORE_API_URL=https://cacstore.com.ng/api
CAC_STORE_API_TIMEOUT=10000

# QoreID API (Phone verification, KYC, Identity services)
# ⚠️  SECURITY: Get NEW credentials from https://qoreid.com/dashboard
# Previous credentials were exposed in .env.example and must be rotated
QOREID_CLIENT_ID=<your-new-qoreid-client-id>
QOREID_SECRET=<your-new-qoreid-secret>

# ============================================
# PAYSTACK CONFIGURATION (Payment Processing)
# ============================================
# ⚠️  PRODUCTION: Use LIVE keys (sk_live_*, pk_live_*)
# Get from: https://dashboard.paystack.com/#/settings/developers
PAYSTACK_SECRET_KEY=sk_live_<your-live-secret-key>
PAYSTACK_PUBLIC_KEY=pk_live_<your-live-public-key>

# Your production domain
APP_BASE_URL=https://yourdomain.com

# ============================================
# SECURITY & AUTHENTICATION
# ============================================
# ⚠️  PRODUCTION SECURITY: ALL must be FALSE
SAVE_PLAIN_KEYS=false
SAVE_PLAIN_KEYS_SQLITE=false
AUTO_IMPORT_KEY=false
RELAX_JWT=false
DEBUG_API_KEY=false
DEBUG_WALLET=false

# JWT Configuration
JWT_ISSUER=business-api
JWT_AUDIENCE=business-api-clients
JWT_EXPIRES_IN=24h

# Authentication
AUTH_ENABLED=true
API_KEY_HEADER=x-api-key

# Logging (info for production, debug only for troubleshooting)
LOG_LEVEL=info

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# CORS CONFIGURATION
# ============================================
# ⚠️  Replace with your actual production domain
CORS_ORIGIN=https://yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com

# ============================================
# CLIENT CONFIGURATION
# ============================================
# Frontend API URL
VITE_API_URL=https://yourdomain.com/api/v1

# ============================================
# DEPLOYMENT
# ============================================
DEPLOYMENT_PLATFORM=firebase

# ============================================
# EMAIL SERVICE (Add when implementing)
# ============================================
# Uncomment and configure when you add email service:
# SENDGRID_API_KEY=<your-sendgrid-api-key>
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_VERIFY_URL=https://yourdomain.com/verify-email

# ============================================
# STRIPE BILLING (Add when implementing)
# ============================================
# Uncomment and configure when you add Stripe:
# STRIPE_SECRET_KEY=sk_live_<your-stripe-secret>
# STRIPE_PUBLISHABLE_KEY=pk_live_<your-stripe-publishable>
# STRIPE_WEBHOOK_SECRET=whsec_<your-webhook-secret>

# ============================================
# PRODUCTION CHECKLIST
# ============================================
# Before deploying, ensure:
# [ ] All <REPLACE-*> placeholders filled with real values
# [ ] QoreID credentials rotated (get new ones)
# [ ] Strong admin password (64+ characters)
# [ ] JWT_SECRET is cryptographically random (64+ hex chars)
# [ ] DATABASE_TYPE=firestore (not memory!)
# [ ] All security flags set to false
# [ ] Paystack using LIVE keys (not test)
# [ ] CORS_ORIGIN matches your production domain
# [ ] NODE_ENV=production
# [ ] This file is NOT committed to git (.gitignore covers it)
